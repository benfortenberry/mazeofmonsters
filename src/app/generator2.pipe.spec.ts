import { Generator2Pipe } from './generator2.pipe';

describe('Generator2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Generator2Pipe();
    expect(pipe).toBeTruthy();
  });
});
